<h1><%= @article.title %></h1>
<p>
	<%= @article.text %>
	<p style="font-size: 12px;">Created at: <%= @article.created_at %></p>
</p>

<h4>All comments:</h4>

<% @article.comments.each do |comment| %>

	<h6>
		<i><%= comment.author %> said:</i>
	</h6>

	<p>
		<i><%= comment.body %></i>
	</p>

	<hr>

<% end %>

<h4>Add comment:</h4>

<p>
	<%= form_for([@article, @article.comments.new]) do |f| %>
	<p>
		<%= f.label :author %>
		<br>
		<%= f.text_field :author %>
	</p>

	<p>
		<%= f.label :body %>
		<br>
		<%= f.text_area :body %>
	</p>

	<p>
		<%= f.submit %>
	</p>

<% end %>

</p>